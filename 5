#include <stdio.h>

int main()
{
	int N;
	printf_s("N: "); //Вывод надписи
	scanf_s("%d", &N); //Ввод информации
	int Y = N / 100; //Вычисляем век, с учетом того, что начало 18 века - 1801 год.
	printf_s("%i\n", Y); //Вывод
	return 0; //Выход
}
