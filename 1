#include <stdio.h>
#include <Math.h>

int main()
{
    int N = 775; //Задаем исходную секунду
    int s = (N % 60); //Вычисляем остаток от целочисленного деления на 60.
    printf_s("%i", s); //Вывод
    return 0; //Выход
}
