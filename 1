#include <stdio.h>
#include <Math.h>

	int main()
	{
		int N; //Задаем исходную секунду
		printf_s("N: "); //Вывод надписи
		scanf_s("%d", &N); //Ввод информации
		int s = (N % 60); //Вычисляем остаток от целочисленного деления на 60.
		printf_s("%i", s); //Вывод
		return 0; //Выход
	}
